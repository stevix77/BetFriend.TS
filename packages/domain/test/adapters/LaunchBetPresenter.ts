import { Bet } from "../../src/bet/Bet";
import { ILaunchBetPresenter } from "../../src/bet/usecases/launchBet/LaunchBetPresenter";

export class LaunchBetPresenter implements ILaunchBetPresenter {
    fail(arg0: { property: string; message: string; }) {
        
    }
    present(obj: Bet) : void {
        
    }
}